# Tasks

## Next

1. Replace per-preview `whisper-cli` launches with one persistent whisper.cpp context while preserving the current provider boundary and final-transcript authority.
2. Sign and notarize Relay.app with a Developer ID, then publish a versioned DMG outside the development-only ad-hoc signing flow.
3. Cache a bounded Accessibility perception snapshot with stable AX identifiers so follow-up element references can remain exact without rescanning the entire frontmost window.

## Later

- Add a Linux `PlatformAdapter` using AT-SPI without changing core orchestration.
- Add optional llama.cpp and MLX `WriterProvider` implementations.
- Add voice confirmation with replay protection and a clear timeout.
- Add site-specific Playwright workflows only where they can be deterministic and verified.
